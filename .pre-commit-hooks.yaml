- id: btp-sec-scan
  name: BTP Security Scan
  description: Scan SAP BTP app package for CVEs, secrets, and BTP misconfigurations
  entry: btp-sec-scan
  language: python
  pass_filenames: false
  always_run: true
  args: [".", "--threshold", "HIGH"]
